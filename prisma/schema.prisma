generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// TODO: 임시유저
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  password String
}

